# Copy to .env.local for local dev (optional - Vite proxy handles same-origin
# dev without this). In Vercel's project settings, add this as a real
# environment variable pointing at your Render backend.

VITE_SERVER_URL=https://your-render-service.onrender.com
